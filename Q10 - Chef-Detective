#include <bits/stdc++.h>
using namespace std;
#define ll long long 
#define Yes cout<<"YES";
#define No cout<<"NO";
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  //Noushil Sharma
  
    map<ll,ll>m;
	ll n;
	cin>>n;
	for(ll i =1; i<=n; i++){
	    ll a;
        cin>>a;
	    m[a]++;
	}
	for(ll i=1; i<=n;i++){
	    if(m[i]==0){
	        cout<<i<<" ";
	    }    
  }
  
  
    return 0;
}

